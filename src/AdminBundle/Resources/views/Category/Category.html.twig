{% extends "@Admin/Default/index.html.twig" %}
{% block body %}
<a href="{{ path("admin_homepage.AddCategory") }}" > Добавить категорию </a>

<ul>
    {% for cat in ListAction %}

        <li> <a href=" {{ path("admin_homepage.categoryproduct" ,{id:cat.id} ) }} " >  {{ cat.category }} </a> </li>
        <a href="{{ path("admin_homepage.CategoryDelete" ,{id:cat.id} ) }}">Удалить</a> </br>

    {% endfor %}
</ul>

{% endblock %}